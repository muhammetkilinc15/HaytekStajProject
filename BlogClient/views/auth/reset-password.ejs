<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("../partials/head") %> 
    <style>
        .reset-password-container {
            display: flex;
            justify-content: center;
            align-items: center; /* Sayfanın üst kısmına yerleştirir */
            align-content: center;
            margin-top: 50px; /* Formu biraz aşağıdan başlatır */
        }
        .reset-password-card {
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 500px;
        }
        .reset-password-header {
            margin-bottom: 1.5rem;
            font-weight: bold;
        }
        .form-control {
            border-radius: 8px;
            padding: 0.75rem;
        }
        .btn-success {
            padding: 0.75rem;
            border-radius: 8px;
        }
    </style>
</head>
<body>   
    <%- include("../partials/nav") %> 

    <div class="container reset-password-container">
        <div class="col-md-12">
            <div class="card reset-password-card">
                <div class="text-center reset-password-header">
                    <h4>Şifre Sıfırlama</h4>
                    <p>Lütfen e-posta adresinizi girin, size şifre sıfırlama bağlantısı gönderelim.</p>
                </div>
                
                <form method="post">
                    <div class="mb-3">
                        <label for="email" class="form-label">E-posta Adresi</label>
                        <input type="email" class="form-control" id="email" name="email" placeholder="E-posta adresinizi girin" required>
                    </div>
                    <div class="d-grid gap-2">
                        <button type="submit" class="btn btn-success">Parolayı Sıfırla</button>
                    </div>
                </form>
            </div>
        </div>
    </div>    
</body>
</html>
