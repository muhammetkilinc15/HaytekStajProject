<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("../partials/head")  %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            background-color: #f8f9fa; /* Arka plan rengi */
        }
        .form-container {
            max-width: 400px; /* Form genişliği */
            margin: auto; /* Ortala */
            padding: 20px; /* İç boşluk */
            background-color: #ffffff; /* Form arka plan rengi */
            border-radius: 8px; /* Kenar yuvarlama */
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* Gölgelendirme */
        }
    </style>
</head>
<body>   
    <%- include("../partials/nav")  %> 
    <div class="container mt-5">
        <div class="form-container">
            <div class="text-center mb-4">
                <h4><i class="fas fa-user-plus"></i> Kayıt Sayfası</h4>
                <p class="text-muted">Lütfen aşağıdaki bilgileri doldurun.</p>
            </div>
            <form method="post">
                <div class="mb-3">
                    <label for="name" class="form-label"><i class="fas fa-user"></i> Kullanıcı adı</label>
                    <input type="text" class="form-control" id="username" name="username" placeholder="Kullanıcı adınızı giriniz" required>
                </div>

                <div class="mb-3">
                    <label for="email" class="form-label"><i class="fas fa-envelope"></i> E-posta</label>
                    <input type="email" class="form-control" id="email" name="email" placeholder="E-posta adresinizi girin" required>
                </div>

                <div class="mb-3">
                    <label for="password" class="form-label"><i class="fas fa-lock"></i> Şifre</label>
                    <input type="password" class="form-control" id="password" name="password" placeholder="Şifrenizi girin" required>
                </div>

                <div class="d-grid">
                    <button type="submit" class="btn btn-success"><i class="fas fa-user-plus"></i> Kayıt Ol</button>
                </div>
                <div class="mt-3 text-center">
                    <p class="text-muted">Zaten bir hesabınız var mı? <a href="/account/login">Giriş yapın</a></p>
                </div>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
